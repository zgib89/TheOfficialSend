// Configuration & presentational mappings
// Export tierClassMap so presentation classes live in a single place
export const tierClassMap = {
    'Budget': { border: 'tier-budget', text: 'text-budget' },
    'Mid-Tier': { border: 'tier-mid', text: 'text-mid' },
    'Premium': { border: 'tier-premium', text: 'text-premium' }
};

// Future config values (colors, affiliate tags) can be added here.
